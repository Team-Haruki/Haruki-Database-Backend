// Code generated by ent, DO NOT EDIT.

package hook

import (
	"context"
	"fmt"
	"haruki-database/database/schema/pjsk"
)

// The AliasFunc type is an adapter to allow the use of ordinary
// function as Alias mutator.
type AliasFunc func(context.Context, *pjsk.AliasMutation) (pjsk.Value, error)

// Mutate calls f(ctx, m).
func (f AliasFunc) Mutate(ctx context.Context, m pjsk.Mutation) (pjsk.Value, error) {
	if mv, ok := m.(*pjsk.AliasMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *pjsk.AliasMutation", m)
}

// The AliasAdminFunc type is an adapter to allow the use of ordinary
// function as AliasAdmin mutator.
type AliasAdminFunc func(context.Context, *pjsk.AliasAdminMutation) (pjsk.Value, error)

// Mutate calls f(ctx, m).
func (f AliasAdminFunc) Mutate(ctx context.Context, m pjsk.Mutation) (pjsk.Value, error) {
	if mv, ok := m.(*pjsk.AliasAdminMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *pjsk.AliasAdminMutation", m)
}

// The GroupAliasFunc type is an adapter to allow the use of ordinary
// function as GroupAlias mutator.
type GroupAliasFunc func(context.Context, *pjsk.GroupAliasMutation) (pjsk.Value, error)

// Mutate calls f(ctx, m).
func (f GroupAliasFunc) Mutate(ctx context.Context, m pjsk.Mutation) (pjsk.Value, error) {
	if mv, ok := m.(*pjsk.GroupAliasMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *pjsk.GroupAliasMutation", m)
}

// The PendingAliasFunc type is an adapter to allow the use of ordinary
// function as PendingAlias mutator.
type PendingAliasFunc func(context.Context, *pjsk.PendingAliasMutation) (pjsk.Value, error)

// Mutate calls f(ctx, m).
func (f PendingAliasFunc) Mutate(ctx context.Context, m pjsk.Mutation) (pjsk.Value, error) {
	if mv, ok := m.(*pjsk.PendingAliasMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *pjsk.PendingAliasMutation", m)
}

// The RejectedAliasFunc type is an adapter to allow the use of ordinary
// function as RejectedAlias mutator.
type RejectedAliasFunc func(context.Context, *pjsk.RejectedAliasMutation) (pjsk.Value, error)

// Mutate calls f(ctx, m).
func (f RejectedAliasFunc) Mutate(ctx context.Context, m pjsk.Mutation) (pjsk.Value, error) {
	if mv, ok := m.(*pjsk.RejectedAliasMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *pjsk.RejectedAliasMutation", m)
}

// The UserBindingFunc type is an adapter to allow the use of ordinary
// function as UserBinding mutator.
type UserBindingFunc func(context.Context, *pjsk.UserBindingMutation) (pjsk.Value, error)

// Mutate calls f(ctx, m).
func (f UserBindingFunc) Mutate(ctx context.Context, m pjsk.Mutation) (pjsk.Value, error) {
	if mv, ok := m.(*pjsk.UserBindingMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *pjsk.UserBindingMutation", m)
}

// The UserDefaultBindingFunc type is an adapter to allow the use of ordinary
// function as UserDefaultBinding mutator.
type UserDefaultBindingFunc func(context.Context, *pjsk.UserDefaultBindingMutation) (pjsk.Value, error)

// Mutate calls f(ctx, m).
func (f UserDefaultBindingFunc) Mutate(ctx context.Context, m pjsk.Mutation) (pjsk.Value, error) {
	if mv, ok := m.(*pjsk.UserDefaultBindingMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *pjsk.UserDefaultBindingMutation", m)
}

// The UserPreferenceFunc type is an adapter to allow the use of ordinary
// function as UserPreference mutator.
type UserPreferenceFunc func(context.Context, *pjsk.UserPreferenceMutation) (pjsk.Value, error)

// Mutate calls f(ctx, m).
func (f UserPreferenceFunc) Mutate(ctx context.Context, m pjsk.Mutation) (pjsk.Value, error) {
	if mv, ok := m.(*pjsk.UserPreferenceMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *pjsk.UserPreferenceMutation", m)
}

// Condition is a hook condition function.
type Condition func(context.Context, pjsk.Mutation) bool

// And groups conditions with the AND operator.
func And(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m pjsk.Mutation) bool {
		if !first(ctx, m) || !second(ctx, m) {
			return false
		}
		for _, cond := range rest {
			if !cond(ctx, m) {
				return false
			}
		}
		return true
	}
}

// Or groups conditions with the OR operator.
func Or(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m pjsk.Mutation) bool {
		if first(ctx, m) || second(ctx, m) {
			return true
		}
		for _, cond := range rest {
			if cond(ctx, m) {
				return true
			}
		}
		return false
	}
}

// Not negates a given condition.
func Not(cond Condition) Condition {
	return func(ctx context.Context, m pjsk.Mutation) bool {
		return !cond(ctx, m)
	}
}

// HasOp is a condition testing mutation operation.
func HasOp(op pjsk.Op) Condition {
	return func(_ context.Context, m pjsk.Mutation) bool {
		return m.Op().Is(op)
	}
}

// HasAddedFields is a condition validating `.AddedField` on fields.
func HasAddedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m pjsk.Mutation) bool {
		if _, exists := m.AddedField(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.AddedField(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasClearedFields is a condition validating `.FieldCleared` on fields.
func HasClearedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m pjsk.Mutation) bool {
		if exists := m.FieldCleared(field); !exists {
			return false
		}
		for _, field := range fields {
			if exists := m.FieldCleared(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasFields is a condition validating `.Field` on fields.
func HasFields(field string, fields ...string) Condition {
	return func(_ context.Context, m pjsk.Mutation) bool {
		if _, exists := m.Field(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.Field(field); !exists {
				return false
			}
		}
		return true
	}
}

// If executes the given hook under condition.
//
//	hook.If(ComputeAverage, And(HasFields(...), HasAddedFields(...)))
func If(hk pjsk.Hook, cond Condition) pjsk.Hook {
	return func(next pjsk.Mutator) pjsk.Mutator {
		return pjsk.MutateFunc(func(ctx context.Context, m pjsk.Mutation) (pjsk.Value, error) {
			if cond(ctx, m) {
				return hk(next).Mutate(ctx, m)
			}
			return next.Mutate(ctx, m)
		})
	}
}

// On executes the given hook only for the given operation.
//
//	hook.On(Log, pjsk.Delete|pjsk.Create)
func On(hk pjsk.Hook, op pjsk.Op) pjsk.Hook {
	return If(hk, HasOp(op))
}

// Unless skips the given hook only for the given operation.
//
//	hook.Unless(Log, pjsk.Update|pjsk.UpdateOne)
func Unless(hk pjsk.Hook, op pjsk.Op) pjsk.Hook {
	return If(hk, Not(HasOp(op)))
}

// FixedError is a hook returning a fixed error.
func FixedError(err error) pjsk.Hook {
	return func(pjsk.Mutator) pjsk.Mutator {
		return pjsk.MutateFunc(func(context.Context, pjsk.Mutation) (pjsk.Value, error) {
			return nil, err
		})
	}
}

// Reject returns a hook that rejects all operations that match op.
//
//	func (T) Hooks() []pjsk.Hook {
//		return []pjsk.Hook{
//			Reject(pjsk.Delete|pjsk.Update),
//		}
//	}
func Reject(op pjsk.Op) pjsk.Hook {
	hk := FixedError(fmt.Errorf("%s operation is not allowed", op))
	return On(hk, op)
}

// Chain acts as a list of hooks and is effectively immutable.
// Once created, it will always hold the same set of hooks in the same order.
type Chain struct {
	hooks []pjsk.Hook
}

// NewChain creates a new chain of hooks.
func NewChain(hooks ...pjsk.Hook) Chain {
	return Chain{append([]pjsk.Hook(nil), hooks...)}
}

// Hook chains the list of hooks and returns the final hook.
func (c Chain) Hook() pjsk.Hook {
	return func(mutator pjsk.Mutator) pjsk.Mutator {
		for i := len(c.hooks) - 1; i >= 0; i-- {
			mutator = c.hooks[i](mutator)
		}
		return mutator
	}
}

// Append extends a chain, adding the specified hook
// as the last ones in the mutation flow.
func (c Chain) Append(hooks ...pjsk.Hook) Chain {
	newHooks := make([]pjsk.Hook, 0, len(c.hooks)+len(hooks))
	newHooks = append(newHooks, c.hooks...)
	newHooks = append(newHooks, hooks...)
	return Chain{newHooks}
}

// Extend extends a chain, adding the specified chain
// as the last ones in the mutation flow.
func (c Chain) Extend(chain Chain) Chain {
	return c.Append(chain.hooks...)
}
