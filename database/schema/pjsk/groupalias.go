// Code generated by ent, DO NOT EDIT.

package pjsk

import (
	"fmt"
	"haruki-database/database/schema/pjsk/groupalias"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// GroupAlias is the model entity for the GroupAlias schema.
type GroupAlias struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Platform holds the value of the "platform" field.
	Platform string `json:"platform,omitempty"`
	// GroupID holds the value of the "group_id" field.
	GroupID string `json:"group_id,omitempty"`
	// AliasType holds the value of the "alias_type" field.
	AliasType string `json:"alias_type,omitempty"`
	// AliasTypeID holds the value of the "alias_type_id" field.
	AliasTypeID int `json:"alias_type_id,omitempty"`
	// Alias holds the value of the "alias" field.
	Alias        string `json:"alias,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*GroupAlias) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case groupalias.FieldID, groupalias.FieldAliasTypeID:
			values[i] = new(sql.NullInt64)
		case groupalias.FieldPlatform, groupalias.FieldGroupID, groupalias.FieldAliasType, groupalias.FieldAlias:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the GroupAlias fields.
func (_m *GroupAlias) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case groupalias.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case groupalias.FieldPlatform:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field platform", values[i])
			} else if value.Valid {
				_m.Platform = value.String
			}
		case groupalias.FieldGroupID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field group_id", values[i])
			} else if value.Valid {
				_m.GroupID = value.String
			}
		case groupalias.FieldAliasType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field alias_type", values[i])
			} else if value.Valid {
				_m.AliasType = value.String
			}
		case groupalias.FieldAliasTypeID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field alias_type_id", values[i])
			} else if value.Valid {
				_m.AliasTypeID = int(value.Int64)
			}
		case groupalias.FieldAlias:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field alias", values[i])
			} else if value.Valid {
				_m.Alias = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the GroupAlias.
// This includes values selected through modifiers, order, etc.
func (_m *GroupAlias) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this GroupAlias.
// Note that you need to call GroupAlias.Unwrap() before calling this method if this GroupAlias
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *GroupAlias) Update() *GroupAliasUpdateOne {
	return NewGroupAliasClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the GroupAlias entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *GroupAlias) Unwrap() *GroupAlias {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("pjsk: GroupAlias is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *GroupAlias) String() string {
	var builder strings.Builder
	builder.WriteString("GroupAlias(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("platform=")
	builder.WriteString(_m.Platform)
	builder.WriteString(", ")
	builder.WriteString("group_id=")
	builder.WriteString(_m.GroupID)
	builder.WriteString(", ")
	builder.WriteString("alias_type=")
	builder.WriteString(_m.AliasType)
	builder.WriteString(", ")
	builder.WriteString("alias_type_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.AliasTypeID))
	builder.WriteString(", ")
	builder.WriteString("alias=")
	builder.WriteString(_m.Alias)
	builder.WriteByte(')')
	return builder.String()
}

// GroupAliasSlice is a parsable slice of GroupAlias.
type GroupAliasSlice []*GroupAlias
